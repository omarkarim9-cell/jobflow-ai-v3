/**
 * SUPABASE REMOVED
 * The application has migrated to Clerk (Auth) and Neon (Database).
 * See services/dbService.ts for the new cloud-sync implementation.
 */
export const supabase = null;
export const isProductionMode = () => false;
export const initSupabaseClient = () => {};